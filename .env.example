# Exemplo de variáveis do FRONTEND (Vite)
# As credenciais do MySQL ficam no backend em: public/api/_config.php (não commitar)

# 1 = usar API PHP + MySQL (Hostinger)
VITE_USE_API=1

# Base da API quando hospedado no mesmo domínio
VITE_API_BASE=/api
