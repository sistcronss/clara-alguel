<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # React Router (SPA) fallback: qualquer rota vai para index.html
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
